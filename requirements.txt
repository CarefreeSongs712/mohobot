# Mohobot — Multi-Bot AI Framework
# OneBot v11 Reverse WebSocket

# Async & Networking
websockets>=12.0
aiofiles>=23.0

# Database (history / memory canonical records)
sqlalchemy>=2.0.0

# Prompt templates (agent subsystem; optional but recommended)
jinja2>=3.0.0

# HTTP/API
httpx>=0.25.0
openai>=1.0.0

# Web Panel
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
python-multipart>=0.0.6
# python-jose[cryptography]>=3.3.0  # Not available on some mirrors, using bcrypt directly
# bcrypt>=4.1.0  # Not on all mirrors; using hashlib as fallback
passlib>=1.7.4
sse-starlette>=1.6.0

# Config
PyYAML>=6.0

# Logging
loguru>=0.7.0

# Image Processing
Pillow>=10.0.0
imagehash>=4.3.0  # 图片 phash 去重(ImageCache)

# CQ Code parsing (regex)
# (stdlib only)

# Vector store (agent memory recall; degrades gracefully if missing)
# 注意: 部分镜像(如清华)无此包, 可用阿里云: pip install chromadb -i https://mirrors.aliyun.com/pypi/simple/
chromadb>=1.5.0

# Song knowledge (agent subsystem; FlashText keyword linker)
# 注意: flashtext 已从 PyPI 下架, 需从 GitHub 安装:
#   pip install --no-build-isolation git+https://github.com/vi3k6i5/flashtext.git
flashtext>=2.7

# VCPedia sync (manual; requests + BeautifulSoup)
requests>=2.28.0
beautifulsoup4>=4.12.0

# System stats (optional — provides detailed /status output)
psutil>=5.9.0

# Type hints
pydantic>=2.0.0
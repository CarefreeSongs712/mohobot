# Mohobot — Multi-Bot AI Framework
# OneBot v11 Reverse WebSocket

# Async & Networking
websockets>=12.0
aiofiles>=23.0

# Database (history)
sqlalchemy>=2.0.0

# Templates (wifepicker 关系图/排行渲染)
jinja2>=3.0.0

# HTTP/API
httpx>=0.25.0
aiohttp>=3.9.0
openai>=1.0.0

# Web Panel
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
python-multipart>=0.0.6
sse-starlette>=1.6.0

# Calendar perception (plugins/perception; plugin degrades if unavailable)
chinese-calendar>=1.10.0
lunarcalendar>=0.0.9

# Optional relationship graph rendering:
#   pip install playwright && playwright install chromium

# Config
PyYAML>=6.0

# Logging
loguru>=0.7.0

# Image Processing
Pillow>=10.0.0
imagehash>=4.3.0  # 图片 phash 去重(ImageCache)

# CQ Code parsing (regex)
# (stdlib only)

# VCPedia sync (manual; requests + BeautifulSoup)
requests>=2.28.0
beautifulsoup4>=4.12.0

# System stats (optional — provides detailed /status output)
psutil>=5.9.0

# Type hints
pydantic>=2.0.0